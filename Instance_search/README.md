Implementing two instance search methods on a dataset of 5,000 images and 20 query instances. 
Each query includes a bounding box. The methods must identify and rank images containing the same instance by similarity or confidence. 
The 20 queries serve as evaluation benchmarks.
